Memory



void equivalent to 0 but can not be used on anything but pointers



size of variable (and the stack) is determined at the compilation time

having a variable set as void makes the compiler not know how much memory to allocate for the variable



pointers to void are valid - these are addresses with fixed memory depending on CPU architecture (4/8 bytes)



Specifiers

1. Auto - makes the variable saved on the stack of its specific function

NB: the value of a variable needs to be initialized.

Auto is never auto initialized.



declare + define are two different things



2\. Static -> goes to stack, lifespan is until the entire lifetime

-stack mechanism of returning a value from a function

-initialized with 0 or an explicit value

-2 types: internals defined in the body of a function and externals defining in the global area





3.Extern - external class variables

c -> obj -> .dll or .lib or .exe

2 steps: definition and declaration

definition - compiler calculates the size

when you put the keyword "extern", the compiler will bypass that allocation. this will notify the compiler that the variable is already declared somewhere, so that the compiler can look for the existing variable



e.g.: 

int a = 5;

extern int b;



int a = 10;

int b = 20;

printf("Value for local b: %d, and for external b: %d", b, ::b);









register variables - direct CPU memory assignment, used for fast computing, skipping RAM completely

&nbsp;		   - used for iterators (indices), cursors

&nbsp;		   - do not have an address as they are placed directly on the CPU



User defined types:

1.Articles - groups different types of primitive variables, all are stored in a contiguous space (compact memory) - example structs

&nbsp;	 - otherwise we can have stack attribute alignment

&nbsp;	 - these types of structures are either packed, or 

2.Bit structures - allows users access at bit level

3.Unions - allow users to share data between the same sections of memory

4.Enumerations - list of constant integers

5.Synonyms - ..



1. Struct - declared > saved on the stack (like the primitive variables)

&nbsp;	  - think of them as arrays of elements with different types

&nbsp; 

obs: struct Student;

student; - access members with dot: student.a; (will not affect other functions)

\&student; - on stack. in order to change this address, you pass it by pointer student\* student;

&nbsp;	  - access members with arrow student->a;



2\. Bit structures 

struct name{

&nbsp;	type field\_name :width;

&nbsp;	int		:width;

&nbsp;	int field\_name  :width;

};





e.g.: changing letters from uppercase to lowercase:

chat letter = 'A';

a) letter+32 = 'a';

b) to\_lower(...);

c) using bit structure to change letter from binary code 01000001 to 01100001 by changing the bit on the 3rd position



hw: send an e-mail with a new example of a bit operation





Bitwise operators:

\& AND (switch off operator 0 turns everything to 0, 1 does not change the value)

| OR (switch on operator, 1 turns everything to 1, 0 does not change the value)

^ XOR (flip operator, if 1 turns 0, if 0 turns 1)

~ One complement (flips bit value)

>> Right shift

<< Left shift



another hw: bitwise operators for pacman problem in slides (xor, and, or)

1 = white, 2 = black





3\. Unions = articles which are optimized for sharing memory



size will be the biggest type in the structure.

typedef union Student{

&nbsp;	int a;	//4B

&nbsp;	char b;	//1B

&nbsp;	short c;//2B	

} Student; //size of union is 4B





Note: MP3 - flipping a bit you can change from STEREO to MONO

